<UserControl x:Class="Zaclip.Control.ClipboardItemCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Zaclip.Control"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Padding="5" CornerRadius="8" BorderThickness="1" BorderBrush="Gray">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- テキスト部分 -->
            <TextBlock
            Text="{Binding Text}"
            Height="40"
            VerticalAlignment="Center"
            TextWrapping="Wrap"
            TextTrimming="CharacterEllipsis"/>

            <!--ボタンエリア-->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />

                </Grid.RowDefinitions>

                <!--保存ボタン-->
                <Button 
                Style="{StaticResource MaterialDesignIconButton}"
                Grid.Row="0"
                Width="30"
                Height="30"
                Background="Transparent"
                Command="{Binding DataContext.PersistCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                CommandParameter="{Binding}"
                BorderThickness="0">
                    <materialDesign:PackIcon Foreground="Black">
                        <materialDesign:PackIcon.Style>
                            <Style TargetType="materialDesign:PackIcon">
                                <Setter Property="Kind" Value="BookmarkOutline" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Persisted}" Value="True">
                                        <Setter Property="Kind" Value="Bookmark" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </materialDesign:PackIcon.Style>
                    </materialDesign:PackIcon>
                </Button>

                <!--削除ボタン-->
                <Button 
                Style="{StaticResource MaterialDesignIconButton}"
                Grid.Row="1"
                Width="30"
                Height="30"
                Background="Transparent"
                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                CommandParameter="{Binding}"
                BorderThickness="0">
                    <materialDesign:PackIcon Kind="TrashCanOutline" Foreground="Black" />
                </Button>
            </Grid>


        </Grid>
    </Border>
</UserControl>
