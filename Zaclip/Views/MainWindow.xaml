<Window
    x:Class="Zaclip.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Zaclip.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Zaclip.Control"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="350"
    Height="450"
    AllowsTransparency="True"
    Background="Transparent"
    MouseLeftButtonDown="Window_MouseLeftButtonDown"
    ShowInTaskbar="False"
    WindowStyle="None"
    mc:Ignorable="d">

    <!--  タブボタンのスタイル設定  -->
    <Window.Resources>
        <Style x:Key="TabRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0,0,0,2" />
            <Setter Property="Padding" Value="15,8" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="BorderBrush" Value="DodgerBlue" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>


    <Border
        Background="White"
        BorderBrush="Gray"
        BorderThickness="1"
        CornerRadius="10">


        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Column="0"
                    FontSize="20"
                    Text="Zaclip" />
                <controls:AccountIcon Grid.Column="2" DataContext="{Binding AccountIconViewModel}" />
                <Button
                    Grid.Column="3"
                    HorizontalAlignment="Right"
                    Background="Transparent"
                    BorderThickness="0"
                    Command="{Binding CloseCommand}"
                    Content="×" />
            </Grid>

            <StackPanel
                Grid.Row="1"
                Margin="0,0,0,5"
                Orientation="Horizontal">

                <RadioButton
                    Command="{Binding ChangeTabCommand}"
                    CommandParameter="Temporary"
                    Content="一時"
                    IsChecked="{Binding IsTemporaryTab, Mode=OneWay}"
                    Style="{StaticResource TabRadioButtonStyle}" />
                <RadioButton
                    Command="{Binding ChangeTabCommand}"
                    CommandParameter="Saved"
                    Content="保存済み"
                    IsChecked="{Binding IsSavedTab, Mode=OneWay}"
                    Style="{StaticResource TabRadioButtonStyle}" />
            </StackPanel>

            <TextBlock
                Grid.Row="2"
                Text="クリップボードの履歴がありません。"
                Visibility="{Binding CurrentListViewModel.HasItem, Converter={StaticResource InverseBoolToVisConverter}}" />

            <ListView
                x:Name="ClipList"
                Grid.Row="2"
                HorizontalContentAlignment="Stretch"
                ItemsSource="{Binding CurrentListViewModel.Items}"
                KeyDown="ClipList_KeyDown"
                MouseLeftButtonUp="ClipList_MouseClick"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                Visibility="{Binding CurrentListViewModel.HasItem, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Margin" Value="0,0,0,5" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <local:ClipboardItemCard />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </Grid>
    </Border>

</Window>
