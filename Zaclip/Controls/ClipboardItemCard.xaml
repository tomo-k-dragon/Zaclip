<UserControl
    x:Class="Zaclip.Control.ClipboardItemCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Zaclip.Control"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Border
        Padding="5"
        BorderBrush="Gray"
        BorderThickness="1"
        CornerRadius="8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  テキスト部分  -->
            <TextBlock
                Height="40"
                VerticalAlignment="Center"
                Text="{Binding Text}"
                TextTrimming="CharacterEllipsis"
                TextWrapping="Wrap" />

            <!--  ボタンエリア  -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />

                </Grid.RowDefinitions>

                <!--  保存ボタン  -->
                <Button
                    Grid.Row="0"
                    Width="30"
                    Height="30"
                    Background="Transparent"
                    BorderThickness="0"
                    Command="{Binding DataContext.PersistCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="{Binding}"
                    Style="{StaticResource MaterialDesignIconButton}">
                    <materialDesign:PackIcon Foreground="Black">
                        <materialDesign:PackIcon.Style>
                            <Style TargetType="materialDesign:PackIcon">
                                <Setter Property="Kind" Value="BookmarkOutline" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Persisted}" Value="True">
                                        <Setter Property="Kind" Value="Bookmark" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </materialDesign:PackIcon.Style>
                    </materialDesign:PackIcon>
                </Button>

                <!--  削除ボタン  -->
                <Button
                    Grid.Row="1"
                    Width="30"
                    Height="30"
                    Background="Transparent"
                    BorderThickness="0"
                    Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="{Binding}"
                    Style="{StaticResource MaterialDesignIconButton}">
                    <materialDesign:PackIcon Foreground="Black" Kind="TrashCanOutline" />
                </Button>
            </Grid>


        </Grid>
    </Border>
</UserControl>
